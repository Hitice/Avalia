@extends('layouts.app', ['title' => 'Nova campanha'])
@section('content')
<div class="mb-6"><h1 class="text-2xl font-semibold text-gray-800 dark:text-white/90">Nova campanha</h1><p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Defina o público e comunique a oferta. Preços continuam definidos no catálogo.</p></div>
<form method="POST" action="{{ route('campanhas.salvar') }}" class="cartao grid gap-5 p-6">@csrf
<div><label class="rotulo-campo" for="nome">Nome</label><input class="campo" id="nome" name="nome" value="{{ old('nome') }}" required></div><div><label class="rotulo-campo" for="oferta">Oferta</label><textarea class="campo" id="oferta" name="oferta" required>{{ old('oferta') }}</textarea></div><div class="grid gap-5 sm:grid-cols-2"><div><label class="rotulo-campo" for="inicio">Início</label><input class="campo" id="inicio" type="date" name="inicio" value="{{ old('inicio') }}" required></div><div><label class="rotulo-campo" for="fim">Fim</label><input class="campo" id="fim" type="date" name="fim" value="{{ old('fim') }}"></div></div>
<fieldset><legend class="rotulo-campo">Empresas elegíveis</legend><div class="grid gap-2 sm:grid-cols-2">@forelse($clientes as $cliente)<label class="rotulo-opcao"><input type="checkbox" name="clientes[]" value="{{ $cliente->id }}" class="text-brand-500 focus:ring-brand-500/20 size-4 rounded border-gray-300 dark:border-gray-700 dark:bg-gray-900"> {{ $cliente->razao_social }}</label>@empty<p class="text-sm text-gray-500 dark:text-gray-400">Cadastre uma empresa antes de definir o público da campanha.</p>@endforelse</div></fieldset><fieldset><legend class="rotulo-campo">Serviços envolvidos</legend><div class="grid gap-2 sm:grid-cols-2">@forelse($servicos as $servico)<label class="rotulo-opcao"><input type="checkbox" name="servicos[]" value="{{ $servico->id }}" class="text-brand-500 focus:ring-brand-500/20 size-4 rounded border-gray-300 dark:border-gray-700 dark:bg-gray-900"> {{ $servico->nome }}</label>@empty<p class="text-sm text-gray-500 dark:text-gray-400">Cadastre um serviço antes de vinculá-lo à campanha.</p>@endforelse</div></fieldset>
<div class="flex gap-3"><x-avalia.botao>Criar campanha</x-avalia.botao><x-avalia.botao variante="secundario" :href="route('campanhas.index')">Cancelar</x-avalia.botao></div></form>
@endsection
