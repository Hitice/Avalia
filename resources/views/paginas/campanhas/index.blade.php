@extends('layouts.app', ['title' => 'Campanhas'])
@section('content')
    <div class="mb-6 flex flex-wrap items-end justify-between gap-3"><div><h1 class="text-2xl font-semibold text-gray-800 dark:text-white/90">Campanhas</h1><p class="mt-1 text-sm text-gray-500 dark:text-gray-400">Ofertas direcionadas a empresas e serviços selecionados.</p></div><x-avalia.botao :href="route('campanhas.criar')">Nova campanha</x-avalia.botao></div>
    @include('paginas.catalogo.avisos')
    <div class="cartao overflow-hidden"><div class="overflow-x-auto"><table class="tabela min-w-[48rem]"><thead class="tabela-cabecalho"><tr><th scope="col" class="px-5 py-3 text-left font-medium">Campanha</th><th scope="col" class="px-5 py-3 text-left font-medium">Período</th><th scope="col" class="px-5 py-3 text-right font-medium">Empresas</th><th scope="col" class="px-5 py-3 text-right font-medium">Serviços</th><th scope="col" class="px-5 py-3 text-left font-medium">Situação</th><th scope="col" class="px-5 py-3 text-right font-medium"><span class="sr-only">Ações</span></th></tr></thead><tbody class="divide-y divide-gray-100 dark:divide-gray-800">@forelse($campanhas as $campanha)<tr><td class="px-5 py-4"><span class="font-medium text-gray-800 dark:text-white/90">{{ $campanha->nome }}</span><span class="mt-0.5 block text-xs text-gray-500 dark:text-gray-400">{{ $campanha->oferta }}</span></td><td class="px-5 py-4 text-gray-600 dark:text-gray-300">{{ $campanha->inicio->format('d/m/Y') }}{{ $campanha->fim ? ' a '.$campanha->fim->format('d/m/Y') : '' }}</td><td class="px-5 py-4 text-right text-gray-600 tabular-nums dark:text-gray-300">{{ $campanha->clientes_count }}</td><td class="px-5 py-4 text-right text-gray-600 tabular-nums dark:text-gray-300">{{ $campanha->servicos_count }}</td><td class="px-5 py-4"><span class="etiqueta {{ $campanha->ativa ? 'etiqueta-sucesso' : 'etiqueta-neutra' }}">{{ $campanha->ativa ? 'Ativa' : 'Encerrada' }}</span></td><td class="px-5 py-4 text-right"><form method="POST" action="{{ route('campanhas.alternar', $campanha) }}" class="inline">@csrf<x-avalia.botao variante="secundario" tamanho="sm">{{ $campanha->ativa ? 'Encerrar' : 'Reabrir' }}</x-avalia.botao></form></td></tr>@empty<tr><td colspan="6" class="tabela-vazia">Crie uma campanha para comunicar uma oferta a empresas selecionadas.</td></tr>@endforelse</tbody></table></div></div>
@endsection
